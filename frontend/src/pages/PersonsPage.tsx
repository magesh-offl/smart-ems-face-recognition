export function PersonsPage() {
    return (
        <div>
            <h2 style={{ color: '#fff', marginBottom: '1rem' }}>Known Persons</h2>
            <p style={{ color: 'rgba(255,255,255,0.6)' }}>
                Manage registered faces in the database.
            </p>
            {/* TODO: Add PersonList and AddPersonForm components */}
        </div>
    );
}
