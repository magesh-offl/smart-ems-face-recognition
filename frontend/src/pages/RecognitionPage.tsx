export function RecognitionPage() {
    return (
        <div>
            <h2 style={{ color: '#fff', marginBottom: '1rem' }}>Face Recognition</h2>
            <p style={{ color: 'rgba(255,255,255,0.6)' }}>
                Upload an image to detect and recognize faces.
            </p>
            {/* TODO: Add BatchUpload and ResultsGrid components */}
        </div>
    );
}
